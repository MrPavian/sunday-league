{
  "name": "sunday-league",
  "private": true,
  "version": "0.0.1",
  "description": "Sunday League – Kreisklasse-Fußball mit Vollamateuren (three.js, HD-Pixelart)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "android:sync": "vite build && cap sync android",
    "android:apk": "npm run android:sync && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/core": "^8.5.2",
    "@fontsource/pixelify-sans": "^5.3.0",
    "three": "^0.186.1"
  },
  "devDependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/cli": "^8.5.2",
    "vite": "^8.3.1",
    "vitest": "^5.0.1"
  }
}
